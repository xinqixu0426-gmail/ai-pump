# AI 开发与维护系统规约 (Agent Directives)

你当前正在辅助开发「水泵 BOM 管理与出图系统」。
请始终遵守以下开发与约定规则：

## 1. 架构红线
- **前端状态管理**：使用 `Zustand`，强制 `stale-while-revalidate` 缓存。在增删改查后续必须调用相关 `fetch` 执行硬刷新（例如 `fetchRecipes(true)`）。
- **组件规范**：全局样式优先在 `theme.ts` 中固化为 Token。在 MUI 中，永远不要将 `<Chip>` / `<div>` 等块级元素嵌套在 `<Typography>` 内，如需搭配，请务必指定 `component="div"`。
- **服务隔离**：所有 SQLite 数据操作仅限 Express (`api.cjs`) 中进行。前端不可引入后端模块或暴露密文配置。API 鉴权必须携带 `credentials: 'include'`。

## 2. 核心模块与微服务端点提示
- 根路由：前端工作端口由于 Vite 配置默认为 `3000`；所有与内网 SQLite 的交互由端口 `3002` 下辖的 `/api/*` 处理。
- `/api/wecom`：处理外部应用通信解密，受极苛刻的响应延时限制（被动答复不超过5s）。调用 LLM 执行大体量检索时必须下发纯文本预响应，然后转为“主动投递”长文本或卡片。
- `/api/rotor` 与 FreeCAD 调度：通过基于 `crypto.randomUUID()` 的机制实现多用户独立并发。不涉及重新计算 3D 拓扑结构，其输出策略严格遵循 `Dimension` 自定义 Label 取代机制。

## 3. 代码生成风格
- 对旧代码进行排错时，保持最小改动原则。
- 提供示例时，请务必剔除环境未兼容的高级库，原生首选。
- 不要解释显而易见的基础框架知识，优先提供立竿见影的代码或执行逻辑。
