执行本计划的唯一任务包：<替换为任务编号，例如N1.1>。

先读prompts/00-Codex总指令.md和05-分阶段实施.md中本包完整要求，以及前置任务的真实交付证据。前置未通过或当前版本已变化时，先重新核对差异，不沿用旧PASS。

只实现本包范围，复用已有业务/目录/执行器；更新字段schema、对应调用方、测试与原权威文档。声明哪些是既有实现、哪些是新增，按API SOP完成。所有源码读取和写入使用UTF-8。

先做本包实施前审计，再编码，再运行适用确定性/真实模型验收；最后做一次独立自审，重点检查原始目标遗漏、错身份、快照口径、未应用覆盖、参数来源、旧新答案双重处理，以及未授权写。测试失败在本包范围内修正并重跑，不降低预期。

任务完成按总指令固定字段返回；列出真正运行和未运行的测试。不得自动push、部署、改生产开关或进入下一包。发现必须扩大业务口径/权限的依赖时准确报告BLOCKED及最小前置，不用新补丁遮蔽。
