# N4.1｜执行提示词

执行任务 **N4.1**，所属阶段：跨业务调查与业务能力缺口。

读取本目录的00-Codex总指令.md，按01-阶段执行模板.md执行；准确任务范围、文件、字段、前置和退出标准，以 ../05-分阶段实施.md 中 **N4.1** 对应小节为准，字段以 ../02-字段合同.md 和 ../contracts/contracts.schema.json 为准。

先核实该包全部前置任务的真实证据和当前Git状态；只处理本包。不能把本计划自检、历史PASS或安全拒绝当成当前业务成功。没有生产权限，不自动push、部署、进入下一任务。完成后提交结构化SUPERVISOR RETURN并停止。
