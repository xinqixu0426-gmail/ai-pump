请开始执行水泵工厂AI-native计划的 **N0.1：只读盘点与差异清单**，不要直接实现新Runtime。

计划包位于项目内 `planning/ai-native-v1/`。先读 `prompts/00-Codex总指令.md`、README、01—06，并遵守仓库AGENTS.md与API契约。计划基准 `251f822491d677056f2d7b82f09473a51321d8e1`，不要重置当前工作树。

这一次只做以下事情：

1. 核对分支、HEAD、dirty、依赖、迁移head、旧内容冻结与历史Git对象是否可用；保护既有改动，不切换生产状态。
2. 沿实际chat→dispatcher→runtime→registry→executor→API→service调用链，核对本计划第03章的现有入口和字段。区分当前成本重建、保存快照、配置覆盖，以及工资/管理费继承。特别核对kit计价、多个线圈方案、报价margin倍率和查询零写边界。
3. 找出已有相同能力；明确N1/N2可复用的符号和需要的最小新增。不能仅凭文件名说没有实现，也不能为了复用重新接入已退役运行器。
4. 交付本地 `planning/ai-native-v1/implementation/N0.1/` 下的baseline.json、api_inventory.json、business-authority-matrix.json、gap-register.json。JSON字段按第05章定义；不放凭据/客户原文。未知生产信息明确UNKNOWN，不向生产写入验证数据。
5. 自查计划是否与当前代码冲突；有则给path:line证据及最小修订，不直接修改成本、业务API或权限。按总指令格式返回PASS/REWORK/BLOCKED和唯一下一任务N0.2，然后停止。

本次不运行生产部署、不改业务代码、不自动push、不自动进入N0.2。无需为了盘点升级依赖、清理文件或新建业务数据库。
