# P16-L-R3 — Generic Collection Semantic Router and Admission Contract

## 1. Executive result

**REWORK; P16_L_PRODUCTION_READY=NO.** Master start/end HEAD `9acf10c4bcd854705affa700670ab2aabe0dc135`. P17 paused. No commit, push, production access/deployment/restart. Prior P16-L/R2 infrastructure, historical evidence and user-owned dirty work are preserved.

Exactly3 frozen semantic passes over the original30-question corpus produced28/30 each, **84/90**, not90/90. The stage correctly stopped before full Answer UAT and post-semantic real detail/answer certification. No prompt/contract adjustment or repeat-until-green followed the run.

## 2. Contract and implementation

Collection Semantic Contract V1 (`collectionSemanticContract.cjs`) defines5 resources and a15-item list/count/detail catalog. Runtime operations also include server-bound continue/ordinal. Filtering is a modifier: NONE/ORDER_STATUS/ORDER_CUSTOMER/ORDER_STATUS_CUSTOMER. Formal status values are unchanged. topN is null or integer1..50 for list only; default20 and maximum50 remain backend-owned. Direct detail requires a supplied exact source ref; unknown references, extra fields, arbitrary operations, unsupported filters, inconsistent field classes and low-confidence model output fail closed.

The existing collection model slot is reused, not an additional stage. Model: existing deepseek-v4-flash configuration, temperature0 through existing client, Tools NONE, max1 semantic call, retry0. Model input contains only raw source, the closed semantic catalog and source-span catalog; no API results, rows, canonical IDs, tools, cursor or conversation identifier. No five-domain phrase processors or UAT sentence branches were added.

Risk model/provider/prompt remain unchanged. A deterministic adapter adds READ_SAFE / WRITE_OR_MUTATION / UNAVAILABLE_OR_UNKNOWN. Validated command means mutation; query/analysis with non-low confidence and no ambiguities means READ_SAFE; unknown/error remains unavailable. needsBusinessData/contextMode are not mutation signals. This class only permits collection interpretation, followed by strict intent admission. Original narrow-read eligibility is retained, including when a request is not collection-applicable. Nonapplicability remains capability coverage failure, not write risk.

After READ_SAFE, anchored bounded continuation/ordinal control grammar uses active authenticated conversation state before new collection model routing. It recognizes control vocabulary with optional politeness/punctuation and bounded decimal counts/ordinals, not domain/business phrases. Appended mixed clauses do not match. Resource/filter/sort/pageSize/query identity remain frozen; changed pageSize is rejected. No original filter reclassification. Risk still runs first and may reject even a syntactically valid continuation.

R2 governed direct identity lookup is retained. Direct and current-page ordinal targets converge through createDetailTarget into resourceType/canonicalEntityRef/detailProjectionId/provenance before the same detail executor. No canonical ID is generated by the model. Collection backend, pagination, count, projections, evidence and answer renderer were not redesigned or tuned.

## 3. Fixed semantic certification

Artifact: `docs/ai-governance/data/p16lr3-semantic-certification.json`.
Harness: `scripts/certify-p16lr3-semantics.cjs`.

All30 source questions are imported unchanged from the original factory. Each repetition uses fixed Oracle context prerequisites for continuation/ordinal, independently of earlier model outcomes. This isolates semantic routing and is **not** a successful chained pagination or Answer UAT simulation. Expected filter values and source targets are compared in memory; only safe class/count/boolean/error metadata is persisted.

| Repeat | Correct | Failed |
|---|---:|---:|
|1|28/30|2|
|2|28/30|2|
|3|28/30|2|
|Total|84/90|6|

All failures:

- L-02, ordinary orders continuation: risk classes UNAVAILABLE_OR_UNKNOWN, WRITE_OR_MUTATION, UNAVAILABLE_OR_UNKNOWN across the3 repetitions. No collection router/model or Tool was invoked. The explicit mutation classification was not overridden by control syntax. This remains a risk-model classification/contract boundary, not a missing pagination implementation.
- L-14, direct customer detail: READ_SAFE, then COLLECTION_SEMANTIC_INVALID in all3 repetitions. No target lookup or detail execution was attempted in this semantic-only harness. Raw model fields were not retained, so a particular wrong ref/field must not be guessed from this safe error code.

The other28 cases match full expected resource/operation/filter/topN/source-target/ordinal semantics in all3 passes. The original4 admission-failure case IDs L-07/L-12/L-13/L-28 now pass12/12 semantic decisions; that does not negate new/remaining L-02/L-14 failures. Direct-detail semantics pass12/15 decisions,4/5 each repetition. Initial orders list and filtered continuation each pass3/3 semantic decisions.

Semantic model calls69; contextual commands bypass the semantic model when risk permits them. Existing risk calls96 (90 positive decisions plus6 negative cases). Additional model stages0. No Business API/Tool/Answer calls in this certification. Freeze-before/after hashes MATCH; historical R2 dataset remains unchanged.

## 4. Negative and deterministic tests

Six real risk-model negative cases cover order deletion, customer/part/recipe/coil modification and mixed read/write. All6 return WRITE_OR_MUTATION; collection router0, semantic model0, execution0, Answer0. Unsafe admissions0. No negative retry.

Focused tests16/16 PASS: strict catalog/schema, both needsBusinessData values, unchanged narrow rejection, contextual control isolation, model bypass, unsupported/tampered filter rejection, ambiguity/incomplete/wrong-family lookup, real fixture HTTP canonical detail5/5, filtered page20+12 with exact no-skip/no-duplicate assertion, ordinary continuation/ordinal, wrong namespace/token/query/TTL and10 overlapping semantic traces. These fixture tests are not post-semantic production or real-model answer acceptance.

Full deterministic regression2265/2266; sole failure known `.guardian/config.yaml` missing. Existing narrow price/inventory/coil/recipe/Exact Entity/owner/fallback suites pass. API contract26/26 PASS. No new deterministic failure. Current-stage full real narrow interception, build and Deep API were not rerun: no backend change/deployment is attempted, and failed semantic gate blocks release. Earlier Deep API failures are not reclassified here.

## 5. Observability, performance and safety

Real semantic certification uses existing observability wrappers with an isolated in-memory tracing adapter. Orphan spans0, unended spans0, cross-request ownership violations0, exact raw-question matches in captured attributes0. The10-concurrent deterministic trace test also passes. This is local trace certification, not a live Phoenix exporter/production telemetry audit. Ordinary provider logs are suppressed; no rows, answers, runtime facts, credentials or tokens are persisted. Semantic dataset contains only permitted structural classes, safe errors, timings and hashes.

Router latency median583.852ms over84 valid decisions including deterministic context commands; model-backed valid router median664.807ms over66 samples. Three rejected risk decisions and three invalid semantic responses lack returned router-duration values and are not included in these medians. Timing is descriptive (deterministic regression overlapped), not a production latency gate. Collection/continuation/detail execution latency is NOT_MEASURED in R3 because full UAT was gated off.

Exact full business UAT sentence scan found0 matches in router/contract/admission production logic. The short continuation control tokens are intentionally present in the approved anchored control grammar, not a question-to-resource lookup. No business keyword handler or UAT identity is present.

V5 Writes0; allowWrite enabling0; Business Mutation Calls From V50; Candidate DB Mutation Successes caused by stage0. All fixture setup is isolated test construction. No production connection, deployment, business-data modification, Legacy restart or P17 work occurred.

## 6. Supervisor boundary and ownership

Release blocker is semantic90/90 failure at L-02 safety classification and L-14 direct-customer semantic contract. Do not force continuation past mutation classification, broaden unknown admission, relax schema, guess identity or retry to obtain a pass. Full answer UAT remains0 for R3; no claimed domain answer accuracies.

Prior Stage/user ownership follows P16-L-RESUME report. R3 adds semantic contract/control modules, semantic certification harness/artifact, focused tests and tracing/wire fixture helpers. It adapts Stage-owned risk/collection entry/intent/admission/detail binding and existing Stage test wire formats. Backend/evidence/continuation storage remain retained. Shared api-reference edits are confined to the collection contract paragraph. No user-owned runtime/package/test hunks were staged, overwritten or committed. No coherent Stage commit is created because PASS was not achieved.

STOP — WAIT FOR SUPERVISOR REVIEW. No production deployment, P16-M or P17.
