# 当前技术债与优化清单

> 命名与引用执行计划更新于 2026-09-16；其余条目保留各自的核对日期。
>
> 本文只保留尚未完成、会影响正确性、维护成本或下一阶段演进的事项。已经完成或失效的整改过程由 Git 历史保存，不继续留在当前文档中。

## 1. 当前结论

2026-08-03 历史审计评分为 87/100，仅保留追溯，不作为当前质量结论；V1 实测证据见 [发布检查清单](deployment-checklist.md)。系统已经具备可靠的模块化单体、正式成本权威、Command/Query 分层、持久幂等、资源版本、事务、强审计和自动化发布门禁。

当前不需要继续为了架构形式拆后端 service。优化重点按顺序是：

1. 按[规格直读命名与全链条引用执行计划](#material-naming-execution)统一自动命名、稳定引用和实时展示；当前已分阶段实施，剩余范围与验收见该节；
2. 补齐新增能力的真实 API 和页面回归；
3. 渐进拆分前端巨型组件，降低修改耦合；
4. 在出现多人协作需求时升级身份与权限模型。

## 2. P2：测试与发布可信度

### 2.1 供应商小套件补齐完整业务链测试

当前已有 Command 归一化、BOM 引擎单元测试，以及 HTTP 创建/读取中的正数数量、未知字段忽略回归；尚未完整覆盖：

- HTTP 修改和重新编辑回填；
- 父型号不存在、组成项超限和并发版本冲突；
- 页面保存、关闭、重新打开和再次保存；
- 窄屏下组成项编辑、删除、错误提示和未保存保护；
- 小套件父项在配方、报价、订单和库存状态中的一致展示。

完成标准：

- 聚焦单元测试通过；
- `npm run verify:api-contract`、`npm test`、`npm run test:deep-api` 和 `npm run build` 通过；
- 至少完成一次真实浏览器桌面和移动端回归，并记录结果。

### 2.2 历史命名兼容退役

当前 Web 业务模型统一使用 `id/createdAt/updatedAt`；零件说明统一为 `remark`，零件目录成本在页面模型中统一为 `catalogUnitCost`。数据库字段、`/api/model-variants`、`modelVariantId/modelName`、capability ID 和部署路径仍是稳定技术标识，不因界面术语调整而改名。

为避免破坏已有 AI、MCP 和外部调用，后端 Row Adapter 暂时保留 `Id/CreatedAt/UpdatedAt`，零件接口暂时保留 `notes` 兼容字段。它们只允许出现在明确的适配器、规范化器和兼容测试中，不得重新进入页面状态或新调用代码。

退役旧别名前必须同时满足：

- 完成仓库内调用方盘点，并确认页面和正式客户端只使用规范字段；
- 结合外部调用和 MCP 运行记录确认兼容字段已无实际消费者；
- 在 `api-reference.md` 标记废弃窗口，并提供至少一个兼容周期；
- 删除旧字段后，API 契约、深层 API、AI/MCP 与完整 Web 构建全部通过。

这项债务不授权直接修改数据库列、公开路径或 capability ID；若确需迁移，必须作为独立 API 兼容任务实施。

## 3. P1：巨型组件瘦身

### 3.1 是否需要瘦身

**需要，但应渐进拆分，禁止一次性重写。**

当前主要巨型组件：

| 文件 | 当前规模 | 主要耦合 |
|---|---:|---|
| `apps/web-next/components/recipes-view.tsx` | 2,515 行（2026-09-07） | 配方列表、草稿编排、模板入口、就地建档、成本/BOM 预览和多类弹层 |
| `apps/web-next/components/quotations-view.tsx` | 1,731 行（2026-09-07） | 报价列表、编辑、成本覆盖、包装配置和转单 |
| `apps/web-next/components/knowledge-view.tsx` | 1,862 行（2026-09-07） | 知识资料、回归评测、反馈处理、向量健康和详情弹层 |
| `apps/web-next/components/ai-view.tsx` | 866 行（2026-09-07） | 页面级编排、对话发送、工具结果回写、工厂配置与知识同步；独立展示和高频输入状态已拆出 |

问题不只是行数，而是一个文件同时承担多个独立变化原因：

- 任一小功能都需要理解整个工作台状态；
- `useEffect/useMemo` 依赖容易形成陈旧闭包或漏刷新；
- 桌面与移动布局在同一渲染树中重复；
- 冲突集中，代码审核难以判断改动是否越界；
- 单元测试只能覆盖 helper，真实交互回归成本持续上升。

### 3.2 瘦身原则

- 以业务职责和状态所有权拆分，不按视觉碎片机械拆小组件；
- 页面容器只保留资源加载、当前工作区选择和跨区协调；
- 表单草稿只能有一个所有者，子组件通过明确 props 和事件修改；
- API 调用继续集中在 `apps/web-next/lib/*`，不得搬进展示组件；
- BOM、成本、库存、状态机和校验权威仍在后端，不以“拆组件”为由复制到前端；
- 每次只迁移一个稳定边界，迁移前后保持 API、文案、键盘操作和移动端行为一致。

规模是预警指标，不作为机械考核：

- 超过约 1,500 行或同时承担 3 个以上独立业务域时，应进入拆分评估；
- 超过约 2,500 行时，不应继续直接叠加大型功能，新增能力应优先抽出稳定边界；
- 新提取的业务区块通常控制在 300～800 行；复杂区块可以更大，但必须保持单一状态责任。

### 3.3 推荐顺序

#### 第一批：`ai-view.tsx`

建议拆出：

- 会话列表与当前会话；
- 消息渲染和工具证据；
- 流式请求状态；
- 写操作确认卡片；
- 附件区；
- 回答反馈、文件归档和知识同步弹层。

写操作确认状态必须继续由服务端 token 和正式回执驱动，不能因组件拆分退化为前端布尔值。

当前进度（第一阶段已结束）：

- 第一批已完成：文件归档、回答反馈、会话删除、知识同步和工厂配置五类弹层迁入 `components/ai/AiWorkspaceDialogs.tsx`；
- API 调用、保存状态和结果刷新仍由 `ai-view.tsx` 持有，展示组件不直接请求接口；
- 第二批已完成：回答依据与确认卡片迁入 `AiAnswerProcess.tsx`，通用结果展示迁入 `AiBusinessResult.tsx`，执行计划展示迁入 `AiWorkflowResults.tsx`，格式化和表格基础迁入 `AiResultPrimitives.tsx`；
- 写操作确认仍使用服务端签发的确认 token，执行计划展示不自行构造业务事实或直接写入；
- 第三批已完成：桌面与移动会话侧栏迁入 `AiConversationSidebars.tsx`，会话加载、打开、删除和搜索状态迁入 `useAiConversationHistory.ts`，流式事件归并、终止控制和消息上下文迁入 `useAiMessageStream.ts`；
- 会话展示组件不直接请求接口；历史状态 Hook 只调用统一 `lib/ai.ts` 客户端，流式状态 Hook 不承载业务事实或写确认；
- 第四批已完成：附件能力读取、上传和临时文件清理迁入 `useAiAttachments.ts`，消息附件与待发送预览迁入 `AiAttachmentDisplays.tsx`，归档查询和保存状态迁入 `AiAttachmentArchiveController.tsx`；
- 临时附件仅在会话消息保存成功后转为持久关联，上传失败和主动丢弃仍清理未引用文件；归档控制器继续通过 `lib/files.ts` 调用正式 API；
- 第五批已完成：消息区迁入 `AiMessageList.tsx`，输入区迁入 `AiComposer.tsx`，语音识别生命周期迁入 `useAiSpeechInput.ts`，回答反馈状态与保存迁入 `useAiAnswerFeedback.ts`；
- 已修复高频输入状态反向牵连整个工作台的问题：草稿、语音和焦点归 `AiComposer` 所有，`AiMessageList` 与共用 Markdown 渲染建立稳定记忆化边界；历史搜索、反馈弹层等无关状态更新不再重复渲染未变化的消息内容；
- `ai-view.tsx` 已由最初 3,409 行降至 836 行；后续新增编排使规模较第一阶段收口时回升，当前仍只保留页面级跨模块协调、标准对话发送、工具结果回写、工厂配置和知识同步；
- 第一阶段到此结束。后续不得仅为降低行数继续机械拆分；只有出现新的独立变化原因、明显状态耦合或测试边界时，才启动第二阶段。

#### 第二批：`recipes-view.tsx`、`quotations-view.tsx` 与 `knowledge-view.tsx`

状态：**可选优化，暂缓实施。**

这三个工作台目前没有已确认的正确性阻塞，继续瘦身的收益主要是降低后续维护成本，不是当前发布前置条件。只有在新增功能明显扩大状态耦合、同一区域频繁产生回归，或单次修改需要跨越多个独立业务区时再恢复：

- `recipes-view.tsx` 按资源加载、草稿编排、模板/目录协调和弹层状态拆分；
- `knowledge-view.tsx` 按资料、评测、反馈治理和运行健康拆分；
- `quotations-view.tsx` 按列表、编辑草稿、成本覆盖和转单流程拆分。

恢复时一次只推进一个工作台，避免并行重构扩大回归面。

### 3.4 瘦身验收标准

- 功能、API、业务结果和当前用户路径不改变；
- 写操作后仍重新拉取对应资源；
- 没有新增裸 `fetch()`、前端成本公式或 snake_case 类型；
- 现有 lint warning 不增加，迁移区的 Hook 依赖 warning 应清零；
- 聚焦测试、全量测试和生产构建通过；
- 桌面、窄屏、键盘操作、弹层滚动和未保存保护完成真实页面回归；
- 拆分后顶层文件只承担编排，不再直接包含全部业务区块实现。

## 4. 条件触发项：身份与权限

当前共享密码和单一 `admin` 角色适合单工厂、少用户的内部系统，不作为当前阻塞项。

出现以下任一条件时，身份与权限升级为 P1：

- 多名员工需要区分报价、采购、库存、技术和管理员权限；
- 需要追溯到具体操作者而不是会话；
- 系统对外提供给其他工厂或远程协作方；
- 需要撤销单个用户权限而不轮换全局密码。

届时应引入独立用户身份、密码哈希、会话撤销、领域权限和操作者审计；不能只增加前端菜单隐藏。

## 5. 文档维护规则

- 本文只记录未完成事项；完成后删除对应条目，由 Git 历史和提交说明保留过程。
- 接口事实仍以 `api-reference.md` 为准，永久规则仍以 `api-contract.md` 为准。
- 产品化模块方案见 `modularization-design.md`，它是未来提案，不代表当前实现。
- 不再恢复已完成的阶段审核、版本说明、迁移计划或重复状态清单。

## 6. V1 AI 助理后续事项

当前已获用户本地人工验收通过，以下不是 V1 已实现能力，也不因本清单自动启动开发：

| 事项 | 当前限制与完成标准 |
|---|---|
| 自由指代记忆与冲突合并 | 当前仅明确句式可靠保存；后续需验证指代来源、冲突确认、失败回执和跨会话隔离 |
| 统一记忆管理 | 反馈候选与个人记忆仍是两条路径；未来统一列表、编辑和生效说明，保持已有记录和版本 |
| 复杂调查覆盖 | 补齐客户分页、页内选择、过期引用和复杂跨业务长链的真实模型矩阵；工具开放不等于所有自然语言场景通过 |
| 回答质量 | 已验收整体可用性，但仍可能冗长或遗漏比较条件；金额检查已覆盖显式货币及表格金额列；仍不能穷尽所有自然语言金额表达和配置理解 |
| 本人确认业务写入 | 当前聊天未启用；须另行明确业务范围后贯通 Preview、确认和正式回执 |
| 生产登录兼容服务自启动 | 2026-09-08 已迁移为 system 域 LaunchDaemon，以 dan 身份运行；进程退出自动恢复、本机和公网实际登录及身份检查均通过，API/Web 与 .env 未变。未执行整机重启验收；3104 兼容程序仍是运行依赖，不能随旧框架删除，操作见运行手册 |
| 发布验收状态 | 历史 4/9、6/9 等失败已经修复并归档；当前补丁、专项结果和回滚均以发布检查清单为准，不据历史数量判断当前版本 |

<a id="material-naming-execution"></a>

## 7. P1：规格直读命名与全链条引用执行计划

> 计划日期：2026-09-15。状态：**规格命名、新建强制生成、现名引用和批量迁移能力已实现；存量核对及未验收范围以7.10为准，未进行生产切换**。
>
> 用户已选择“规格直读”方向，要求零件被模板、配方、订单、采购等引用后，名称全链条实时更新。用户随后授权新建分支并开始实施，当前开发分支为 `codex/spec-naming-live-references`；该授权不包含生产数据改名或发布。用户于2026-09-15确认旧称可以弃用：不实现旧称搜索或别名续接；稳定 ID 负责关联，现名用于显示，原始单据快照和审计保留历史内容。本节保留未完成清单，不能视为已上线功能说明。

### 7.1 目标、范围与使用方法

最终用户流程：选择类别、填写实际规格，系统生成标准名称；规范同一物料的叫法后，所有相关业务页面显示新名称，引用、库存、采购进度和已锁定业务事实连续有效。

范围包含零件、泵壳套件、供应商小套件、线圈方案、模板和配方的名称；引用链覆盖新建/编辑/复制/就地建档、BOM、成本、报价、订单、采购、库存、默认配件、搜索、导出、AI/MCP 和关联资料标签。历史常用配置入口仍在时也必须覆盖。

本次不新增独立整泵成品库存，不合并供应商库存，不替换物料实物规格，不改成本公式，不扩大 AI/MCP 写权限，不进行无关组件重构。

执行顺序固定为：**P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7 → P8**。P0—P7 在开发环境及隔离数据库完成；正式改名与生产切换仅在 P8 进行。每阶段应形成可审查提交，记录实际命令、结果和剩余阻塞。未满足阶段通过条件不得标记完成。

复用现有 SQLite、正式 capability、Preview/Command、幂等、版本检查、事务、强审计、业务变更记录和 API 客户端。开发前完整阅读 [API 契约](api-contract.md)、[API SOP](api-sop.md)，发布和恢复遵循 [备份恢复](database-backup-recovery.md)及[运行手册](operations-runbook.md)。

### 7.2 已核实的起点

本地核对基线：Git `bb9d7f7`，SQLite schema `82`；未删除零件 133 条、配方 4 条、模板 2 条、线圈方案 10 条、定子组合 3 条、订单 2 条、报价 2 条。这不是生产环境基线；开始实施和正式切换前都要重新盘点，不硬编码下一迁移版本。

| 当前问题 | 代码或数据依据 | 必须覆盖的修复 |
|---|---|---|
| 普通零件改名没有全链条传播 | `partRenameImpact.cjs` 已拦截有已知引用的直接改名，完整迁移仍待接入 | 统一身份与名称读取，不为每类页面增加独立字符串替换 |
| 配方和模板存在大量纯名称引用 | 当前 4 个配方普通 BOM 行、模板配件与组件没有 `partId` | 唯一核实后补齐引用；无法确定时返回待核对状态 |
| ID 与旧名称同时参与身份判定 | `bomPartIdentity.cjs`、`orderPlanning.cjs`、`purchaseInventory.cjs` | 区分显示名变化与实体/规格变化，不能只补 ID 后保留旧名相等校验 |
| 配方库存身份查询已修复，完整改名授权未接入 | `recipeInventory.cjs`、`recipeQueries.cjs:getInventoryStatus` | ID或唯一型号/供应商查询，异常单列；下一步接受保护改名和规格修订 |
| 采购项重建依赖名称及旧键 | `orderPlanning.cjs`、`orderWorkflow.cjs`、`orderQueries.cjs` | 固定采购行身份并保留配置维度及全部进度 |
| 成本和选项生成会解析型号文字 | `costEngine.cjs`、`recipeBomEngine.cjs`、`part-form-rules.ts` | 从结构化规格和明确角色读取，名称仅用于展示/搜索 |
| 部分页面已共享业务版本轮询 | 配方工作区编辑期间暂缓回读，关闭后补刷 | 其余页面及真实双浏览器验收仍待完成 |

### 7.3 必须保持的业务不变量

1. **物料身份不随名称改变。** 同一零件使用原 `partId`，线圈使用原 `coilId/schemeCode`；模板、配方保留原 ID。不得删除重建以完成改名。
2. **标准名称只有一个权威来源。** 由后端根据类别规则和结构化规格生成。前端预览、网页保存、AI、MCP、批量导入和就地建档调用同一规则。
3. **名称不是业务条件。** 匹配、成本角色、库存、类别、采购合并和参数推导不再靠名字里的“长螺丝”“线径”等字样判断；迁移期旧字符串解析只留在显式兼容适配层。
4. **规格变化不等于改名。** 5×12 换为 5×20 必须选择/新建另一物料并走明确替换；同一实物资料录错用受控纠错。规格修订与名称修订分开，不能仅删除旧名称校验而放松防错。
5. **当前业务页统一显示现名。** 包括已关闭订单和历史报价的日常查看页；原始快照、修订历史、审计内容、已生成文件原文保持原样，必要时另示“当时名称”。
6. **纯改名不产生业务数量/金额变化。** 库存、预留、用量、已下单/到货/入库数量、实际采购价、锁定成本、销售价、订单状态不变；不因刷新名称重算并保存订单。
7. **供应商与配置边界保留。** 同规格不同供应商继续保留各自物料身份；同一基础电缆的不同长度、接头、单位等保持不同采购配置，不能仅按 `partId` 合并。
8. **用途名称独立。** “提手螺丝”“压板螺丝”等 BOM 角色不跟着物料名改写。小套件只绑定、计价、扣减父物料；非库存费用不强造零件 ID。
9. **读操作无写副作用。** 当前名称装配在内存完成；不在 GET、页面加载、AI 查询中回填 ID、改快照、同步知识或生成审计。
10. **实时刷新保护草稿。** 名称更新不得覆盖未提交的数量、供应商、选配或备注；过期写预览仍按正式协议拒绝并重新预览。

### 7.4 数据与契约设计基线（当前实现见7.10）

采用增量改造，不引入新的 ERP 平台或微服务。优先沿用 `parts.model`、`coils.scheme_name`、`recipes.name` 等现有名称载体；禁止再维护另一份可以独立编辑、与标准名称冲突的显示名。

| 数据职责 | 建议实现 | 约束 |
|---|---|---|
| 命名输入 | 按类别登记且受 schema 校验的规格对象、命名规则版本、命名状态 | 规则版本影响已有名称时走显式预览/迁移，不在读取时静默改名；已有 `remark` 业务元数据保留 |
| 实物规格身份 | 规范化后的关键规格及独立规格修订 | 名称修订不能改变实物规格身份；字段范围在 P1 按类别固定 |
| 标准名称 | 后端生成后存入对象现有名称字段 | 名称冲突时检查规格差别与供应商，不随机追加“副本2” |
| 原厂/对外型号 | 与内部生成名分开记录，报价/订单冻结各自对外型号 | 型号与客户品牌均保留；TOKOY已确认为客户品牌，使用独立对外型号承载，不据品牌猜实物规格 |
| 模板绑定 | 套件模式增加明确泵壳 `partId`；固定配件、组件、默认配件保存类型化引用 | `shellModel` 不再同时承担模板显示名与物料关联键；多供应商不按最低价猜选 |
| 历史名称 | 原单据快照与审计保留当时名称 | 旧称弃用，不搜索、不作为关联键；已有别名表暂不启用，不做破坏性删表 |
| 当前读取结果 | 区分 `currentName`、`snapshotName`、类型化 ID、解析状态及名称版本 | 字段名在能力契约中冻结；旧响应字段语义不得直接从快照偷换为现名 |
| 新业务引用 | JSON/BOM/明细保存正式 `partId/coilId/recipeId/templateId` 与必要的配置身份 | 前端展示优先现名，写入提交 ID 和业务版本，显示字符串不是写目标 |
| 旧快照绑定 | 使用独立、可审计的历史引用绑定记录，关联原对象/版本/JSON 位置/源内容哈希与目标 ID | 保持已锁定 JSON 原文与金额不变；源版本或位置变化时原绑定失效，禁止套用到另一行 |
| 实时通知 | 复用持久业务变更事件及单调游标，通知实体类型/ID/版本 | 无需广播整份订单；名称提交后可立即从正式 Query 读到 |

旧快照绑定属于正式迁移结果；新保存的业务数据直接带正式 ID，不重复维护第二套权威关联。用于“被哪些对象引用”的反向索引可以派生，但必须可重建，不能替代正式引用。

命名示例采用 `品名-规格-必要区别`。分类规则至少覆盖轴承、机械/骨架油封、螺丝、泵壳、泵壳搭配、浮球、电缆、电容、皮垫、配件、包装、线圈、模板及配方。维度内统一 `*`，型号字母大写，片数明确写“片”；物理单位按已核实含义规范化：用户已确认电缆“线径0.55”表示横截面积0.55mm²；浮球及绕组线径不套用此解释。定子俗称“12”保留与 120mm 的已确认映射。

配方内部识别采用“成品型号＋必要结构/配置区别”，配置摘要由完整规格自动生成；同名异配置必须补充真实区别。采购父物料、模板和最终配置名称分别生成，不能因为共用某泵壳就把不同成品型号合并。

新规则下必填规格缺失阻止正式新建；存量不完整记录显示“待规范化”，仍可按已核实 ID 查询或履行既有业务。非标件通过“品名＋明确规格＋必要区别”结构化入口录入，不能成为任意修改整串标准名称的后门。

### 7.5 分阶段执行状态

原计划的 P0→P8 顺序和通过条件继续有效。以下按当前实现更新，避免把已实现功能继续列为待接入；阶段通过不能替代存量业务核对和生产授权。

| 阶段 | 已完成的代码与隔离证据 | 尚未关闭的验收条件 |
|---|---|---|
| P0 引用盘点 | 类型化引用盘点、嵌套 JSON 位置、候选规格、来源哈希和缺失/歧义状态；字段矩阵见7.11 | 含义不明存量对象需要人工核实；正式批次前重新冻结业务基线 |
| P1 模型与契约 | 16条公共规则、规格身份档案、名称/规格修订、模板独立泵壳绑定、配方对外型号；能力登记和schema83–85 | 无新增代码接入项；正式数据需审核后迁移 |
| P2 公共命令与现名 | 受保护改名、批量现名装配、版本与来源校验、事务、幂等、审计、提交事件、公共目录缓存刷新；批量命名复用同一命令 | 含歧义或缺失引用的对象保持阻塞，不能算已规范化 |
| P3 业务链 | 模板、配方/BOM、报价、订单、采购、库存和出图按有效ID/绑定装配现名；采购行ID、进度、原始快照及价格保留；新建默认轴承和长螺丝定价来源存具体ID | 尚未审核的存量引用不得自动绑定或合并；完整验收范围见7.8、7.10 |
| P4 录入与展示 | 零件各类、线圈、模板、配方、常用配置、复制及就地建档由服务端生成名称；套件选择具体泵壳；配方名称预览独立于折叠区域，自动使用当前定子/片数/明确长度 | 真实页面结果及未运行矩阵据实记录在7.10 |
| P5 刷新与AI/MCP | 正式业务版本读取、页面回读、编辑草稿保护；AI创建配方走结构化规格和正式保存草稿；保存回读比较原始快照，现名投影不降低业务校验 | 两个真实浏览器全矩阵和真实模型验收需独立证据，HTTP会话不能替代 |
| P6 迁移与恢复 | 显式批量预览/迁移API（1–100条已核对记录），全批来源冻结、原子事务、幂等、强审计；相交引用、失败整体回滚及正式备份恢复已在隔离库演练 | 完整存量映射和固定价格下的全量业务不变量比较，待业务核对后实施 |
| P7 门禁与文档 | 针对性测试、契约、全量测试、deep API、lint、构建及MCP隔离闭环；当前事实归入原权威文档 | 未完成的真实环境矩阵不能勾为通过；最新命令及范围见7.10 |

P0–P7的技术接入不代表全部存量已经改名。未知规格、歧义供应商和失效来源都由正式命令拒绝；不从名称猜测、不删除、不合并。P8必须单独确认生产范围。

#### P8：正式切换、生产验收与计划收口

- [ ] 独立确认正式部署及数据迁移范围后，重新盘点生产数据，按权威流程生成绑定旧提交的 release 备份并验证；不能沿用本地 133 条等数量。
- [ ] 安排停止业务写入的切换窗口，控制所有 Web/AI/MCP/内部写入口，源版本变化则作废旧映射并重新预览；采用现有部署/维护机制，不擅自开放绕过路径。
- [ ] 按“兼容代码与 schema → 引用迁移 → 校验 → 名称规范化 → 当前展示/通知验收”的顺序执行。生产数据变更只使用正式维护/命令服务，动态 UPDATE 走 `safeUpdate()`。
- [ ] 发布前通过 `npm run verify:release` 及既有生产发布门禁；核对库存、采购进度、原始快照、关键成本和全链条名称，正常后恢复业务写入。
- [ ] 失败时先停止写入、保存当前安全备份和失败证据，按 `database-backup-recovery.md` 联合恢复代码与数据库；若已恢复业务写入并产生新交易，不直接覆盖旧备份，应先核对并保全新增交易。
- [ ] 对恢复连接的旧页面清理过期缓存；旧表单/确认凭证受版本检查保护，不得把旧名重新写回。
- [ ] 记录实际生产提交、schema、迁移 run、备份与验收结果。完成项归入权威业务章节及 Git 历史，本节只保留尚未完成的事项；删除 README 中待实施入口时同步检查链接。

通过条件：真实生产全链条显示新名称、身份及业务数据保持连续，发布与恢复证据可核对，所有待规范化/未解析项按批准范围处理完毕。

### 7.6 阶段提交与交接格式

每阶段可拆多个小提交；提交应围绕已完成的正式能力及验证证据，不能把“名称 UI 已改”当作整个功能完成。每次暂停或交接记录：

```text
阶段：P…；状态：未开始 / 进行中 / 已通过
实际修改：文件、能力、schema、兼容边界
验证：执行命令、结果、证据位置；未运行项及原因
数据：仅隔离库 / 正式维护 run；业务不变量核对结果
阻塞：需核实物料或失败链路
下一步：下一条具体任务与通过条件
```

### 7.7 最低能力清单

| 能力职责 | 分类与调用方 | 必需保护 |
|---|---|---|
| 分类规则/字段查询 | Query；Web/内部，AI 按需要登记 | 分页/版本、只读、未知类别错误 |
| 生成名与冲突/引用影响预览 | Preview；Web/AI/MCP 正式调用链 | 复用命令校验，返回规范化规格、候选与冲突，不落库 |
| 保存主数据/规范名称 | Command；复用已有对象能力 | 版本、幂等、预览绑定、事务、强审计、正式回读 |
| 当前名称解析、按 ID 引用查询 | Query；所有正式读取方 | 类型化 ID、唯一性、完整性状态、批量查询及无写副作用 |
| 事件游标/实时事件读取 | Query/stream；Web | 现有鉴权、断线重连、保留窗口、限流与连接回收 |
| 引用回填与批量命名 | 显式 Maintenance；内部授权执行 | 源版本/哈希、审核映射、dry-run、幂等、批次事务、审计、备份及恢复 |

确切 capabilityId、输入/输出 schema、HTTP 或 INTERNAL trigger 以当前注册表和API文档为准；不得出现“有脚本所以不用登记”“AI 能直接读写库”“前端先改再补审计”等旁路。

### 7.8 最终验收矩阵

| 维度 | 必须覆盖的案例 | 断言 |
|---|---|---|
| 命名生成 | 每类物料、空/非法规格、单位、大小写、非标、重复与冲突 | 所有入口同一结果；不能靠改空格制造新物料 |
| 改名类型 | 普通件、长螺丝、电缆/浮球、电容、包材、泵壳、小套件、线圈、模板、配方 | 原 ID 不变；每条适用引用链名称一致 |
| 名称与实物边界 | 单纯改名、规格替换、规格纠错、停用、删除 | 纯改名连续；错误实物/停用引用不能继续执行 |
| 引用解析 | 无 ID、有 ID＋弃用旧名、重名/多供应商、零候选、部分缺失 | 唯一事实才绑定；失败与歧义不冒充零库存 |
| 采购身份 | 8m/10m 电缆、不同接头、不同单位、不同供应商、同规格多线圈方案 | 不错误合并；旧采购行 ID、进度、单位换算正确 |
| 订单生命周期 | 待确认、待采购、部分下单/到货/入库、采购完成、已关闭、已取消 | 适用页面全部显示现名；纯改名不改变状态与进度 |
| 全流程 | 模板→配方→报价→订单→部分采购→改名→继续入库→领用关闭 | 无新缺料/缺价、无重复任务、无错扣/重复入库 |
| 业务不变量 | 库存/流水、预留、BOM 数量、锁定金额、采购实际值、原始快照哈希 | 纯改名和引用迁移前后相等；新名称元数据与审计变化在预期清单内 |
| 当前成本 | 固定同一目录价/铜价/设置下的全部正式成本入口 | 改名前后结果一致，未知不能按零计价 |
| 实时更新 | 同页、两个标签页、两个独立会话、快速连续改名、断线/重启/游标过期 | 正常联网目标≤2秒；重连补齐，不重复业务写入 |
| 草稿与并发 | 未保存输入、旧请求、过期确认、并发改名/入库、幂等重放 | 草稿不丢；版本冲突明确；不能执行过期或错误目标 |
| 历史与资料 | 已关闭订单日常页、原始快照、修订历史、旧 PDF、关联标签、当前 CSV | 当前页/新清单显示现名；原始内容不改写 |
| AI/MCP/知识 | 新名/旧称、同名候选、已改名历史会话、旧知识候选、查询失败 | 正式 ID 与现名正确；无猜测绑定、无权限扩张 |
| 维护可靠性 | 重复迁移、源数据漂移、事务中断、映射冲突、恢复演练 | 不半批落库，不抹历史，不覆盖新交易 |

### 7.9 仍需业务核对的内容

SPA、QDXSS的水泵型号身份、128的采购识别尺寸和TOKOY客户品牌含义已于2026-09-16确认。以下剩余疑点不会阻止公共机制开发，但会阻止对应对象的正式改名/合并。具体ID、目录名称及26条隔离演练候选见[存量名称核对清单](catalog-naming-review.md)：

2026-09-15 用户已确认：纯名称整理保留 ID，实物规格变化新建记录；同规格多供应商继续分别管理库存。6202对应202、6203对应203，其他标准6开头轴承同理，出图仍用标准代号。电缆、浮球旧“线径”均表示横截面积（mm²）。电缆目录价格为该规格的元/米，成本=每米单价×米数+配件费用。

2026-09-15阶段记录：本地 pump.db 中5个泵壳的 defaultUpperBearing 已由6202改为202，事务内写入审计3206–3210，操作前备份在 backups/before-bearing-202-1789479232967.db；逐表对比52张业务表，除指定备注字段与 updated_at 外保持原值，当时schema为82（后续结构变化见7.10）。当时只读盘点剩12处缺失引用、1处非结构化关联。电缆命名规则限定截面积/mm²，录入、配方、线圈及 AI 文案已明确单位和每米计价。旧电缆目录字符串仍保留，未宣称完成全链改名。验证：2091项测试、26项契约、474项deep API、lint及构建通过；隔离副本真实页面已检查电缆录入单位与相邻布局。

- `SPA 3 叶` 与 `SPA 3叶轮` 是否为同物；当前不能仅凭相似名称合并。
- QDXSS已确认为水泵型号，128为泵壳关键尺寸和采购识别规格；仍需核对第三件“128泵垫/128油板”是否同义及正确名称，不猜量纲。
- `v1100-2` 的数字2、包材 `v1100DF`、部分油封不完整尺寸与图纸连续数字含义。
- TOKOY已确认为客户品牌，原型号通过独立对外型号保留；V750两个配方已有175/180mm机筒差异，不能因去掉品牌或名称统一合并。
- 测试零件与测试模板是否继续保留、停用或独立管理；本计划不授权自动删除。

命名规则、引用迁移、实时展示和验收四部分任一缺失，本项不视为完成。

### 7.10 当前实现与剩余验收

开发分支为 `codex/spec-naming-live-references`。阶段过程以Git历史为准；只读盘点 `complete` 不等于所有引用已核实。

规则集版本4提供16类规则，零件、线圈、模板、配方和常用配置新建均使用服务端生成名称。模板与常用配置必须提供系列/配置的结构化命名输入；配方名称中的定子代号、片数和明确的机筒长度来自保存参数，不能填入不一致规格。长度未知时省略，不猜填。AI创建配方使用相同规则；原输入型号作为对外型号保留，后续安全改名不覆盖。普通编辑不能绕过规范名称与实物规格保护，实物规格/供应商变更需另一物料ID。

改名使用正式预览、同物确认、版本、全来源哈希和规格修订校验，同一事务提交名称、档案、绑定、幂等回执、强审计和业务事件。批量迁移API接受1–100条已核对输入，先完整验证冻结的全批来源，再在一个事务内调用正式改名命令；相交引用可以连续续接，中途失败整批回滚。已演练正式备份与隔离恢复，不代表源库已批量迁移。

业务读取通过显式ID或有效唯一绑定装配现名，不替换原始JSON。套件模板独立绑定泵壳ID；新建泵壳默认轴承明确供应商对应的零件ID，旧代号仅唯一时绑定。动态长螺丝保存定价来源ID，有ID失效时不回退另一供应商或公式。电缆/浮球面积为mm²，电缆按元/米×长度+配件计价，库存按米；变更面积不继承旧ID。报价/订单保留对外型号，采购行ID、数量、进度、锁定金额、历史修订和原始快照不改写。

Web新增和复制由规格表单生成名称；模板编辑按绑定ID显示真实泵壳。配方预览在草稿层运行，折叠基础区域后仍跟随定子、片数及长度变化。相关页面按已提交业务版本重新读取，编辑期间保护未保存草稿。模板删除使用软删除，保留身份档案/绑定；有档案的线圈删除转为停用，保留历史关联，停用对象不能用于新业务。模板及常用配置名称受数据库全局唯一约束，历史记录占用的名称不能重新使用，创建/改名返回409而非数据库500。AI修改配方回读比较 `snapshotPartsJson/snapshotExtraPartsJson/snapshotPackingPartsJson/snapshotConfigurationPolicyJson`，当前标签投影不会误报保存失败，实际快照漂移仍拒绝报成功。

2026-09-16最终代码验证：2124/2124测试、26/26 API契约、488项完整deep API、lint及构建通过；MCP验证304项矩阵测试与18/18真实MCP→API→SQLite写入通过，外部打印/FreeCAD使用隔离替身，不代表物理验收或真实AI。隔离源库副本的正式Express与最终Next构建已验收：配方创建生成12/160片/180mm名称并保存独立客户型号；基础区域折叠后名称仍跟随片数；复制配方清空旧名称并展开必填规格；模板自由搭配创建与身份档案落库；改名后另一标签页自动显示现名，编辑页保留草稿，放弃草稿后读取现名。两个标签页属于同一个真实浏览器，不代表两种独立浏览器或≤2秒时限已验收。真实AI查询已通过正式聊天API尝试，局域网模型重试后仍返回EHOSTDOWN/AI_PROVIDER_NETWORK_ERROR，尚未通过。证据位于被忽略的 `logs/catalog-naming/remaining-*`。此前3700c9d已有正式改名→独立HTTP会话现名→继续采购→幂等入库和原始快照/库存连续性证据；本轮完整deep的外部铜价请求通过，43e63e8的失败仅为历史记录。

开发schema为85。本地源库因旧AI测试遗漏测试环境设置，误加载触发了82→84的追加结构迁移；逐表与 `backups/before-bearing-202-1789479232967.db` 比较，除此前已授权的5个泵壳默认6202→202及审计3206–3210，业务数据保持一致。源库未执行本轮批量命名或schema85；对比证据为 `logs/catalog-naming/remaining-source-comparison.json`。公共数据库入口现已识别node测试上下文并使用隔离路径，防止直接运行测试再次触达源库。

2026-09-16补充存量演练：源库隔离副本中26条轴承/电容/电缆/浮球通过正式批量迁移服务；50张非允许变化表、业务基线、未选零件与已选零件的非命名字段比较一致，4个保存BOM的当前目录配件成本相等，幂等重放通过。不是源库正式迁移或全部成本场景验收，具体清单见 `catalog-naming-review.md`。只读盘点仍有7处旧来源标记和1处非结构化图纸关联。

剩余是存量含义/缺失规格/歧义引用核对、其余存量批次的不变量比较，以及未取得证据的真实环境矩阵。P8的生产部署、数据迁移和公网验收尚未授权、未执行。旧称可以弃用，但历史快照和审计继续保留；未知物料不猜规格、不合并、不删除。


### 7.11 引用链矩阵与迁移边界

以下为代码已定位的主要路径，作为后续逐链验收清单；表中“迁移策略”尚未全部实现。

| 资源字段 | 写入方 | 读取方 | 业务用途与现有身份 | 迁移策略 |
|---|---|---|---|---|
| 零件 `model/remark`，默认轴承与长螺丝定价来源 | `partCommands`、批量/就地建档 | `recipeQueries`、`costEngine`、出图参数 | 主名称、默认配置、定价规则；混用 ID 和字符串 | 规格与用途分离；默认件和定价来源显式 ID；旧词弃用，保留快照与审计 |
| 模板 `shell_model`、整套泵壳 | `templateCommands`、零件改名依赖保护 | `pumpShellPartResolver`、`recipeBomEngine`、`templateQueries` | 既是模板名又是物料匹配字符串 | 独立模板名＋`catalog_template_shell_bindings`；不再靠模板名关联泵壳 |
| 模板 `parts_json/shell_components_json` | 模板编辑/复制、`templateCommands` | BOM 展开、成本、配方默认值 | 固定件、组件、供应商套件；旧行无 ID | 保留已有 ID、审核后补绑定；套件内部描述不建库存 |
| 配方 `parts_json/extra_parts_json/packing_parts_json` | `recipeCommands`、保存草稿、Web 选配与包装 | `recipeQueries`、`currentRecipeCost`、`configuredRecipeSnapshot` | BOM、成本、库存；新 ID 与旧名称快照并存 | 新写保存 ID；旧快照旁路绑定，现名与原名分开输出 |
| 配方/模板配置规则白名单 | `recipeConfigurationPolicy`、对应保存命令 | 配置覆盖、包装选项 | `packingPartIds`、线圈 ID 数组 | 原 ID 保留，选项标签读取现名；不扩大可选范围 |
| 线圈/定子组合 | `coilCommands`、方案编辑 | `coilCost`、BOM、采购和线圈库存 | `coilId/schemeCode`、规格快照、方案族 | 保留具体方案身份；命名不改电气参数、片数或方案默认选择 |
| 常用配置 `pump_model_variants` | `modelVariantCommands`、旧入口 | BOM/出图、配方默认值 | 配置 ID、线圈 ID 与型号快照 | 保留入口；名称与参数默认值分别迁移 |
| 报价 `items_json`、附件来源 | `quotationCommands/quotationDraft` | `quotationQueries/quotationConversion`、导出 | 配方 ID、嵌套 BOM、对外型号和锁价 | 展示现名；原报价内容与金额保持，转单复用绑定身份 |
| 订单 `items_json` 及修订快照 | `orderCommands/orderBomSnapshot` | `orderQueries/orderReadiness`、订单导出 | 嵌套快照与配方 ID，包含字符串化 JSON | 历史绑定核验源哈希，订单修改续接 ID；不改修订正文 |
| 采购 `purchase_list_json` | `orderPlanning/orderWorkflow`、采购命令 | `orderQueries/purchaseInventory` | `partId/coilId/identityKey` 及名称退路 | ID＋长度/接头/单位区分；原行 ID、收货回执及进度不得重建丢失 |
| 库存、预留与入库 | `purchaseInventory`、库存命令、订单状态机 | 采购平衡、配方库存、准备度 | 主库存 ID，预留由活动订单需求派生 | 公共身份校验；价格/显示名称变化不改变预留及扣减对象 |
| 转子关联文本、文件关联标签 | 转子档案/文件归档命令 | 出图历史、文件归档目标查询 | `linked_pump_model` 文本、文件类型化目标 ID | 文本保留原文，关联标签用现名；非结构化文本不猜绑定 |
| 搜索、AI/MCP、知识、导出 | 正式 API client 与各导出入口 | `entityLookupService`、AI executors、MCP | 目录查词、ID 回执、历史知识候选 | 正式查询使用现名或固定 ID；知识与历史消息不提供写身份；原文件不回写 |
